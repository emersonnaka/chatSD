/* * Sistemas Distribuídos * Profº. Rodrigo Campiolo * Emerson Yudi Nakashima 1451600 * TCPClient: Cliente para conexao TCP * Descrição: Envia uma informacao ao servidor e finaliza a conexao */import java.io.EOFException;import java.io.IOException;import java.net.Socket;import java.net.UnknownHostException;public class TCPClient {	public static void main (String args[]) throws InterruptedException {		/* args[0]: ip do servidor */		if (args.length == 1){			Socket connectionSocket = null;			try{				String serverAddress = args[0];				int serverPort = 7896;   /* especifica a porta */				connectionSocket = new Socket(serverAddress, serverPort);  /* conecta com o servidor */  								System.out.println("Conectado");				new SendReceiveThread(connectionSocket);								connectionSocket.close();			} catch (UnknownHostException e){				System.out.println("Socket:"+e.getMessage());			} catch (EOFException e){				System.out.println("EOF:"+e.getMessage());			} catch (IOException e){				System.out.println("leitura:"+e.getMessage());			} //catch		} else {			System.out.println("Insira no comando de execução: ip_do_servidor");		}	} //main} //class